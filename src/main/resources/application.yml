eureka:
  client:
    register-with-eureka: false
    fetch-registry: false
    service-url:
      defaultZone: http://dfss.staging.co.zw:8701/eureka/
  server:
    wait-time-in-ms-when-sync-empty: 1

spring:
  jmx:
    default-domain: service-discovery-server
  application:
    name: service-discovery-server

server:
  port: 9000